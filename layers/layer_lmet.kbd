;; "" LMET_LAYER """"""""""""""""""""""""""""""""""""""""""""""""""""""""""""
(deflayermap (lmet_layer)
  (t! map_layers "$action_lmet" "lmet" ($<lctl $>lctl $<lalt $>lalt $lsft $rsft $!lctl $!lalt lsft rsft))
  (t! autobind_keys_except   "$action_lmet"        ($<lctl $>lctl $<lalt $>lalt $lsft $rsft tab))

  $invert|keyb (t! invert_apps_sort $toggle_lmet_layer)
  ;; lmet+tab mimics original lctl+tab functionality (typically next tab in 
  ;; browsers and editors).
  tab $action_tabs+l
) 

(deflayermap (lmet+lsft_layer)
  (t! map_layers "$action_lmet+lsft" "lmet+lsft" ($<lctl $>lctl $<lalt $>lalt $!lctl $!lalt))
  (t! autobind_keys_except   "$action_lmet+lsft"             ($<lctl $>lctl $<lalt $>lalt tab))

  $invert|keyb (t! invert_apps_sort $toggle_lmet+lsft_layer)
  tab $action_tabs+h
)

(deflayermap (lmet+rsft_layer)
  (t! map_layers "$action_lmet+rsft" "lmet+rsft" ($<lctl $>lctl $<lalt $>lalt $!lctl $!lalt))
  (t! autobind_keys_except   "$action_lmet+rsft"             ($<lctl $>lctl $<lalt $>lalt tab))

  $invert|keyb (t! invert_apps_sort $toggle_lmet+rsft_layer)
  tab $action_tabs+h
)
