(defvar

;; == files/folders ========================================================
;; files/folders + spc
;; == #files/folders ========================================================
;; #files/folders + spc
tmux_action_seek_and_select                   (macro $tmux_prefix w) ;; prefix + w (choose window)
;; file/folder + lsft + spc
;; ___TO_SET___ tmux_action_seek_and_select+lsft   
)
