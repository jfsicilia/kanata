(defvar

;; == seek_sel ========================================================
;; seek_sel + spc
dolphin_action_seek_and_select                  C-l
;; seek_sel + lsft + spc
dolphin_action_seek_and_select+lsft            (push-msg "NOTIFY:Seek and select folders not implemented!") 
;; seek_sel + rsft + spc
dolphin_action_seek_and_select+rsft            (push-msg "NOTIFY:Seek and select folders not implemented!") 
)
