(defvar
  ;; Lang layer
  toggle_keyb_layer        (multi (t! toggle_layer keyb_layer)
                                  (on-press tap-vkey vk_apps_sort_toggle) 
                                  (on-release tap-vkey vk_apps_sort_toggle)
                           )
)
